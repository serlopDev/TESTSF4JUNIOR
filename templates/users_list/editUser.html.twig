{% extends 'base.html.twig' %}

{% block body %}

    <div class="content">
        <div class="edit-user-box">
            <form class="edit-form">
                <h1 class="title-edit-user">Editar datos de: {{user.name}}</h1>
                {{form(editForm)}}
            </form>
            <a id="access-ef" class="cancel-button-u" href="{{path('users_list')}}">CANCELAR</a>
        </div>
    </div>    

{% endblock %}